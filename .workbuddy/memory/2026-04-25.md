# 2026-04-25

- 完成了农事作业模块中“灌溉记录”、“剪枝记录”、“打孔结香”和“采收香木”页面的真实 API 对接：
  - 创建了 `IrrigationViewModel`、`PruningViewModel`、`PunchViewModel` 和 `HarvestViewModel`。
  - 将各页面的模拟提交逻辑替换为调用 ViewModel 中的真实网络请求，并添加了提交状态的监听（Loading 动画、成功 Toast 提示及返回上一页）。
  - 处理了各页面中复杂的表单校验逻辑，确保按需传递 `plantQrcode`、`fieldQrcode` 或 `fieldCode` 等参数。
  - 修复了 `SubmitState` 类缺失导致的编译报错。
  - 统一修复了所有 ViewModel 中 `RetrofitClient` 的调用方式报错（将错误的 `createService` 或 `instance.create` 统一修正为 `businessRetrofit.create`）。
  - 统一修复了所有 ViewModel 中错误引用 `response.msg` 的问题（`BaseResponse` 中的字段名实际为 `message`）。
  - 修复了 `PlantingEntryScreen` 和 `SaplingEntryScreen` 页面加载时没有触发品种列表和苗床列表的网络请求的问题，将 `fetchInitialData()` 调用从 ViewModel 的 `init` 移至 UI 的 `LaunchedEffect(Unit)` 中。
  - 统一修复了项目中所有 ViewModel 中对于网络请求成功状态码的判断，将 `code == 0` 统一修改为兼容模式 `code == 0 || code == 200`，解决了因状态码判断不一致导致下拉框数据无法显示的问题。
  - **网络安全升级**: 后端域名已部署 HTTPS 证书。将 `RetrofitClient` 中的 `BUSINESS_BASE_URL` 从 `http://dbcx.org.cn/` 升级为 `https://dbcx.org.cn/`。
    - 撤回了之前为解决 HTTP 明文请求报错而在 `AndroidManifest.xml` 中添加 `networkSecurityConfig` 的方案，并删除了相应的 `network_security_config.xml` 文件，因为 HTTPS 默认受 Android 系统信任。
  - **药肥信息录入对接**: 完成了 `AgInputManagerScreen.kt` 中农药、肥料、供应商三个子页面的 UI 与 `AgInputViewModel` 的绑定。
    - 替换了供应商下拉框数据源，从本地模拟数据切换为调用后端接口获取的真实数据。
    - 修改了保存按钮逻辑，将硬编码的 Toast 提示替换为调用 ViewModel 对应的提交方法（`submitSupplier`, `submitPesticideInput`, `submitFertilizerInput`），并通过 `LaunchedEffect` 监听 `SubmitState` 统一处理成功和失败的 UI 反馈。
    - 为农药和肥料页面添加了基于 `ActivityResultContracts.GetContent()` 的图片选择器，并将选中的图片 `Uri` 转换成临时的 `File` 对象，实现了图片上传与表单数据的打包提交。