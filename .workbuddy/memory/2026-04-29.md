- 2026-04-29: 全面清理了农事作业模块（包括施药、灌溉、病虫害、定植、幼苗等页面）中遗留的 `simulateScan` 和 `simulateMotherTreeScan` 模拟扫码逻辑，彻底替换为基于 CameraX 和 ZXing 的 `ScannerScreen` 真实扫码组件，并修复了 Material 3 的 `menuAnchor` 废弃警告。
- 2026-04-29: 在灌溉记录录入页面接入了 `plantationList` 和 `seedbedListByGh` API，实现种植园、大棚、苗床的层级联动选择；修复了幼苗培育录入页面在无空闲苗床时下拉框显示空白的问题，改为显示不可选的"无空闲苗床"；确认了药肥入库页面已经集成了 `fertilizerSupplierList` 和 `pesticideSupplierList` API，并正确处理了供应商的选择逻辑。
- 优化了 `SaplingEntryScreen.kt`（幼苗培育录入页面）中苗床下拉框的 UI 交互：在大棚下无空闲苗床导致下拉框被禁用时，提示文字固定显示为“请选择空闲苗床”（不再显示“无空闲苗床”），以符合用户的 UX 偏好。

- 修正：根据用户的确认，将 `SaplingEntryScreen.kt` 中的空闲苗床下拉框恢复为：在大棚下无空闲苗床导致下拉框禁用时，占位符文字显示为“无空闲苗床”。

- 修正：根据用户的最新反馈，将 `SaplingEntryScreen.kt` 中空闲苗床下拉框的行为修改为：当选择大棚后无空闲苗床时，下拉框保持可点击状态，占位符显示为“请选择空闲苗床”，但点击展开后的下拉菜单中显示唯一的选项“无空闲苗床”（点击此项不改变实际选中的值）。
