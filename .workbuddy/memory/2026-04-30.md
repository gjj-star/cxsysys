
- 文档与同步：重写并更新了项目根目录下的 `README.md` 文件，详细补充了项目的分层架构（api、model、ui、viewmodel 等）和 `app/src/main/java/com/example/cxsysys` 目录下各类页面的具体职能说明。
- 将更新后的 README.md 提交并同时 push 到了 Gitee (`origin`) 和 GitHub (`github`) 两个远程仓库。
